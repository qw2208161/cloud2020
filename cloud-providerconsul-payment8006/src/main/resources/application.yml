###consul服务端口号
server:
  port: 8007

spring:
  application:
    name: consul-provider-payment
  ####consul注册中心地址
  cloud:
    consul:
      host: 101.34.64.62
      port: 8500
      discovery:
        service-name: ${spring.application.name}
        prefer-ip-address: true
        health-check-interval: 30s
        hostname: 101.34.64.62
management:
  endpoint:
    health:
      show-details: always

